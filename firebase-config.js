/* ------------------------------------------------------------------
 * firebase-config.js — paste your Firebase web app config here.
 *
 * Firebase Console -> Project settings (gear) -> Your apps -> Web app
 * -> "SDK setup and configuration" -> Config. Copy the values across.
 *
 * These values are NOT secret. Firebase publishes them in every web app;
 * `apiKey` here is a project identifier, not a password, and it is safe in a
 * public repo. What actually protects your data is Firestore security rules
 * (only you can read your own document) — see README.
 *
 * Leave this file as-is and the app simply runs without cloud sync,
 * exactly as before, storing everything on the device.
 * ------------------------------------------------------------------ */

window.FIREBASE_CONFIG = {
  apiKey: 'AIzaSyDd5kaJnq5-NSJZoKkAhs7Dpkqe5tuL4LA',
  authDomain: 'su3rh-abdullah.firebaseapp.com',
  projectId: 'su3rh-abdullah',
  storageBucket: 'su3rh-abdullah.firebasestorage.app',
  messagingSenderId: '965266495929',
  appId: '1:965266495929:web:0b8f240a5e6ea4630cfe68',
};
